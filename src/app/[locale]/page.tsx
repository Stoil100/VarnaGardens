import MainButton from "@/components/MainButton";
import Image from "next/image";

export default function Home() {
  return (
   <main className="h-screen w-full">
    <MainButton>Виж повече</MainButton>
   </main>
  );
}
